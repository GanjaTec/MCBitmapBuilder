package de.jml.external.util.math;

import de.jml.external.util.ArrayTask;
import de.jml.external.util.Matrix;
import org.jetbrains.annotations.NotNull;

public class VMatrix<T> implements Matrix<T> {


    @Override
    public void forEachCol(@NotNull ArrayTask<T> doForEach, boolean parallel) {

    }
}
